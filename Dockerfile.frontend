# Use Nginx image to serve the frontend
FROM nginx:alpine

# Copy the frontend code (HTML) into the Nginx server
COPY index.html /usr/share/nginx/html/index.html

# Expose the frontend port (default is 80)
EXPOSE 80
